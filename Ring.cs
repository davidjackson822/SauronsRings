namespace Application {
    abstract class Ring implements IRing
    {
        public enum metal
        public enum<array> gem, powers
        public string inscription
        public int size

        public Ring() {
            this.metal = metal;
            this.gem = gem;
            this.powers = powers;
            this.inscription = inscription;
            this.size = size;
        }
    }
}